import { Row, Sort, SortType } from "./DataSource";
export declare class SortBy implements Sort {
    readonly name: string;
    readonly type: SortType;
    constructor(name: string, type?: SortType);
    handler(a: Row, b: Row): number;
}
export declare class Then<T extends Sort, E extends Sort> implements Sort {
    readonly name: string;
    readonly type: SortType;
    private readonly _a;
    private readonly _b;
    constructor(a: T, b: E);
    handler(a: Row, b: Row): number;
}
