import { Formatter, KeyValue, Row } from "../Datasource";
export declare enum ColumnType {
    string = "string",
    int = "int",
    float = "float",
    boolean = "boolean",
    date = "date",
    currency = "currency"
}
export interface Column {
    title: string;
    prefix?: string;
    dataIndex: string;
    suffix?: string;
    lookup?: ((value: any) => string) | KeyValue<string>;
    dateOut?: string | ((value: string) => string);
    type: ColumnType;
    decimal?: number;
    thousandSeparator?: boolean;
    editable?: boolean;
    filter?: boolean;
    sort?: boolean;
    format?: (row: Row, key: string, defaultFormat: string) => string;
}
export interface Table {
    columns: Column[];
    tableName: string;
    search?: boolean;
    add?: boolean;
    delete?: boolean;
    edit?: boolean;
    select?: boolean;
    refresh?: boolean;
}
export declare const formatter: (column: Column) => Formatter;
