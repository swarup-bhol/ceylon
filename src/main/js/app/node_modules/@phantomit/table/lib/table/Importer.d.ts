import React from "react";
import { ColumnConfig } from "./def";
import { Row } from "@phantomit/datasource/lib/Datasource";
export interface Schema {
    [key: string]: number;
}
export declare enum RecordState {
    pending = 0,
    rejected = 1,
    completed = 2
}
export declare enum ImportAction {
    none = 0,
    uploading = 1,
    importing = 2
}
export interface Record {
    row: Row;
    state: RecordState;
    __id__: number;
}
export interface ImporterProps {
    columns: ColumnConfig[];
    save: (row: Row) => Promise<boolean>;
    visible?: boolean;
    onClose: () => void;
}
export declare const mapToRow: (columns: ColumnConfig[], values: any[]) => Row;
export declare const Importer: React.FunctionComponent<ImporterProps>;
